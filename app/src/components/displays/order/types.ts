import { ImageSizeData } from "../image/types"

/*export type OrderData = {
  name: string,
  size: string,
  price: string,
  in_stock: boolean,
  selectedImage: string,
  imageSize: ImageSizeData | null,
  rotated: boolean,
  productId: number,
}*/
